{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "prisma generate && prisma db push --accept-data-loss && bun run db:seed && next build",
  "installCommand": "bun install",
  "framework": "nextjs",
  "regions": ["cdg1"],
  "env": {
    "DEMO_MODE": "false"
  }
}
